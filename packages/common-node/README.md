# `common-node`

Shared node.js library.

This should _not_ be imported from any web packages. Use common-web for that.

The one exception is dashboard-web, which I think needs an esm build to function.